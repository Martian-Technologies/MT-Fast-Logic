[project]
name = "mt-fast-logic-tools"
version = "0.1.0"
description = "Development tools for MT Fast Logic"
requires-python = ">=3.11"
dependencies = [
    "fonttools>=4.55",
    "numpy>=2.1",
    "pillow>=11.0",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
